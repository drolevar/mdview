# Dark theme

Headings, paragraph, code, and a diagram. The harness opens this file
with `lcp_darkmode` set so the initial paint is dark.

```python
def hello():
    return "hi"
```

```mermaid
flowchart LR
    A --> B
```

Some text.
