# Many math expressions

A doc with more than 8 math placeholders to exercise the worker
path (math-chunk.ts short-circuits to sync render when total
placeholders ≤ 8).

Inline math line 1: $a$, $b$, $c$, $d$, $e$.

Inline math line 2: $f$, $g$, $h$, $i$, $j$.

Display math 1:

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

Display math 2:

$$
\int_{0}^{1} x^2 \, dx = \frac{1}{3}
$$

Display math 3:

$$
e^{i\pi} + 1 = 0
$$

Display math 4:

$$
\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}
$$
